# 命理意图补齐复测报告

日期：2026-02-26  
关联报告：`docs/09-命理回归报告-2026-02-26.md`

## 1. 本轮改动

针对 5 条“命理意图漏判”样例，补齐了后端识别规则：

1. 扩展主命理关键词：新增 `命理`、`提运`
2. 新增弱意图规则：
   - 场景词：`今天/本周/这周/本月/最近/这段时间/现在/未来`
   - 决策词：`最旺方向/行动方向/避免决策/换岗积累/更容易提运` 等
3. `is_bazi_fortune_query` 改为：
   - 主关键词命中即走命理路由
   - 或“场景词 + 决策词”组合命中也走命理路由

## 2. 复测命令

```bash
python /app/scripts/fortune_regression.py --base-url http://127.0.0.1:8000 --max-cases 24 --timeout 90
```

## 3. 复测结果

- 总用例：24
- 通过：24
- 失败：0
- 通过率：100.00%

说明：此前失败的 5 条（FORTUNE-011/012/014/015/016）本轮全部转入命理结构化链并通过断言。

## 4. 指标快照（`/quality/metrics?days=1`）

- `fortune_route_hit_rate`: `1.0`
- `fortune_tool_success_rate`: `1.0`
- `fortune_field_completeness_rate`: `1.0`
- `profile_echo_violation_rate`: `0.0`
- `template_repeat_rate`: `0.0617`

## 5. 结论

“命理意图漏判”问题已在当前回归集中修复。  
建议将本次规则纳入后续发布基线，并继续按 `scripts/fortune_regression.py` 做每次上线前回归。

